// Code generated by Wire. DO NOT EDIT.

//go:generate go run github.com/google/wire/cmd/wire
//go:build !wireinject
// +build !wireinject

package main

import (
	"github.com/go-kratos/kratos/v2"
	"github.com/kwstars/film-hive/app/rating/service/internal/biz"
	"github.com/kwstars/film-hive/app/rating/service/internal/conf"
	"github.com/kwstars/film-hive/app/rating/service/internal/data"
	"github.com/kwstars/film-hive/app/rating/service/internal/server"
	"github.com/kwstars/film-hive/app/rating/service/internal/service"
	"go.opentelemetry.io/otel/sdk/trace"
)

import (
	_ "go.uber.org/automaxprocs"
)

// Injectors from wire.go:

// initApp init kratos application.
func initApp(bootstrap *conf.Bootstrap, tp *trace.TracerProvider) (*kratos.App, func(), error) {
	logger := server.NewLogger()
	registrar, err := server.NewNamingClient(bootstrap)
	if err != nil {
		return nil, nil, err
	}
	dataData, cleanup, err := data.NewData(bootstrap, logger)
	if err != nil {
		return nil, nil, err
	}
	ratingRepo := data.NewRatingRepo(dataData, logger)
	ratingUsecase := biz.NewRatingUsecase(ratingRepo, logger)
	ratingService := service.NewRatingService(ratingUsecase)
	grpcServer := server.NewGRPCServer(bootstrap, ratingService, logger)
	httpServer := server.NewHTTPServer(bootstrap, ratingService, logger)
	app := newApp(logger, registrar, grpcServer, httpServer)
	return app, func() {
		cleanup()
	}, nil
}
