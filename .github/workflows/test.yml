name: Hello World!

on:
  workflow_dispatch:
  push:
    branches:
      - main
  pull_request:

jobs:
  say-hello:
    runs-on: ubuntu-22.04
    steps:
      - name: Checkout
        uses: actions/checkout@v3
      - name: Say Hello World!
        run: echo "Hello world"